---
- name: Run PowerShell script on Windows servers
  hosts: WindowsTaskServer
  gather_facts: no
  connection: winrm

  tasks:
    - name: Execute PowerShell script
      win_shell: |
        # Your PowerShell script content goes here
        # For example:
        Invoke-Command -ScriptBlock {
          # Your PowerShell script commands here
          # For example:
          Get-Service
        }
