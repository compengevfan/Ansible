---
# - name: GetCredentials
#   hosts: AlmaLinux
#   gather_facts: no
#   tasks:
#     - name: Obtain Credentials
#       include_role:
#         name: GetVaultCreds
#       vars:
#         includecred:
#           - linuxroot
#       run_once: true
        
- name: Run the ConfigureLinux Role
  hosts: AlmaLinux
  gather_facts: yes
  tasks:
    - name: Run the ConfigureLinux Role
      include_role:
        name: ConfigureLinux